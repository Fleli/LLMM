
#pragma once

typedef int LLLValueCase;

/* Assigning to a variable in the local scope. */
#define ll_lvalue_variable

/* Assign to an address pointed to by a variable in the local scope. */
#define ll_lvalue_pointer
